# Refer for explanation to following link:
# https://lefthook.dev/configuration/

# pre-push:

pre-commit:
  parallel: false
  jobs:
    - name: repo checks
      run: bash ./util-scripts/pre-commit.sh
